{
  "name": "sai-website",
  "version": "1.0.0",
  "description": "SAI Fire Alert System Website",
  "scripts": {
    "start": "node server.js",
    "start-static": "cd static && python3 -m http.server 8080 --bind 0.0.0.0",
    "dev": "NODE_ENV=development node server.js",
    "configure": "node scripts/configure-webhook.js",
    "build": "node scripts/build-config.js",
    "setup-security": "node scripts/setup-security.js",
    "test-webhook": "curl -X POST http://localhost:8080/api/test-webhook"
  },
  "dependencies": {
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.0",
    "helmet": "^6.1.5",
    "cors": "^2.8.5",
    "node-fetch": "^3.3.1"
  },
  "license": "CC-BY-SA-4.0"
}